/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/models/source/merged_animations_3_clips.glb --transform --types --shadows --output src/models/generated/MergedAnimations3Clips.tsx 
Files: public/models/source/merged_animations_3_clips.glb [7.28MB] > public/models/merged_animations_3_clips-transformed.glb [496.8KB] (93%)
*/

import * as THREE from 'three'
import { useEffect, useMemo, useRef } from 'react'
import { useGraph, type ThreeElements } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { SkeletonUtils, type GLTF } from 'three-stdlib'

export type MergedAnimationClip = 'Dead' | 'Skill_01' | 'Idle_03'

interface GLTFAction extends THREE.AnimationClip {
  name: MergedAnimationClip
}

type GLTFResult = GLTF & {
  nodes: {
    char1: THREE.SkinnedMesh
    Hips: THREE.Bone
  }
  materials: {
    Material_1: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

type ModelProps = ThreeElements['group'] & {
  animation?: MergedAnimationClip
  loop?: boolean
}

export function MergedAnimations3Clips({
  animation = 'Idle_03',
  loop = true,
  scale = 100,
  ...props
}: ModelProps) {
  const group = useRef<THREE.Group>(null)
  // The optimized artifact is retained for review, but it requires Draco. Loading
  // the untouched source keeps this generated graph usable on restricted machines.
  const { scene, animations } = useGLTF('/models/source/merged_animations_3_clips.glb')
  const clone = useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as unknown as GLTFResult
  const { actions } = useAnimations(animations, group)

  useEffect(() => {
    const action = actions[animation]
    if (!action) return

    action.reset()
    action.enabled = true
    action.clampWhenFinished = !loop
    action.setLoop(loop ? THREE.LoopRepeat : THREE.LoopOnce, loop ? Infinity : 1)
    action.fadeIn(0.2).play()

    return () => {
      action.fadeOut(0.2)
    }
  }, [actions, animation, loop])

  return (
    <group ref={group} scale={scale} {...props} dispose={null}>
      <group name="Scene">
        <group name="Armature" scale={0.01}>
          <primitive object={nodes.Hips} />
        </group>
        <skinnedMesh name="char1" geometry={nodes.char1.geometry} material={materials.Material_1} skeleton={nodes.char1.skeleton} scale={0.01} />
      </group>
    </group>
  )
}

useGLTF.preload('/models/source/merged_animations_3_clips.glb')
